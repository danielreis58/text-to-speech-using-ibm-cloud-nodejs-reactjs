const host = 'localhost'
const port = 3306
const user = 'daniel'
const password = 'daniel123'
const database = 'smartkio'

module.exports = {
    host: host,
    port: port,
    user: user,
    password: password,
    database: database
}